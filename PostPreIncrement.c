#include<stdio.h>
int main(){ 
    int x=5;
    printf("%d\n",x);
    printf("%d\n",x++);//x++ means; use x, the increment
    //++x means,increment then use
    // printf("%d\n",++x);

    return 0;
}